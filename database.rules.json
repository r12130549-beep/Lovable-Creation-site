{
  "rules": {
    "admins": {
      ".read": "auth != null",
      ".write": "false",
      "$uid": {
        ".read": "auth != null && auth.uid == $uid"
      }
    },
    "orders": {
      ".read": "auth != null && root.child('admins').child(auth.uid).exists()",
      ".write": "true"
    }
  }
}